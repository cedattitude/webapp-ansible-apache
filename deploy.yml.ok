---
- name: "Apache installation using docker"
  hosts: prod
  tasks:
  - name: "Launch docker container"
    docker_container:
      name: "webapp"
      image: "httpd"
      state: started
      ports:
        - "80:80"
